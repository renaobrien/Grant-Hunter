The button primitive. Presentational — wire behavior in a client island.

```jsx
<Button variant="primary">Save settings</Button>
<Button>Secondary</Button>
<Button size="sm" href="/">← Board</Button>
```

`variant`: primary (brand-filled) or secondary (bordered, default). `size`: md or sm. Pass `href` to render an identically-styled `<a>`.
