Centered placeholder for an empty list, board column, or table. Mandatory — never render a bare empty container.

```jsx
<EmptyState
  title="Nothing here yet"
  hint="Grants land here as they move through the pipeline."
  action={<Button variant="primary">Run discovery</Button>}
/>
```

`hint` and `action` are optional.
