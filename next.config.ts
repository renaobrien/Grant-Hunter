import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev-tools overlay in the corner during `npm run dev`.
  devIndicators: false,
};

export default nextConfig;
